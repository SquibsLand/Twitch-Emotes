{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Vite Build",
            "type": "shell",
            "command": "Remove-Item -Recurse -Force dist; npm run local-build"
        },
        {
            "label": "Vite Build GitHub",
            "type": "shell",
            "command": "Remove-Item -Recurse -Force dist; npm run build"
        }
    ]
}